{
  "name": "git-organized",
  "version": "0.1.0",
  "description": "Keep git repos organized",
  "bin": {
    "git-organized": "lib/index.js"
  },
  "dependencies": {
    "chalk": "^1.1.1",
    "nodegit": "^0.22.2"
  },
  "scripts": {
    "build": "babel src -d lib",
    "watch": "babel -w src -d lib",
    "flow": "flow; test $? -eq 0 -o $? -eq 2",
    "lint": "eslint src"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-eslint": "^6.0.0",
    "babel-plugin-syntax-flow": "^6.5.0",
    "babel-plugin-transform-flow-strip-types": "^6.7.0",
    "babel-preset-es2015": "^6.6.0",
    "eslint": "^2.5.1",
    "flow-bin": "^0.22.1"
  }
}
